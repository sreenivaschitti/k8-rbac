apiVersion: v1
kind: Pod
metadata: 
  name: aws-cli
  namespace: roboshop

spec:
  serviceAccountName: roboshop-secret-reader
  containers: 
  - name: aws-cli
    image: amazon/aws-cli
    command: ["sleep","2000"]


---

# eksctl utils associate-iam-oidc-provider --cluster roboshop --approve

# eksctl create iamserviceaccount \
#     --name roboshop-secret-reader \
#     --namespace roboshop \
#     --cluster roboshop \
#     --attach-policy-arn arn:aws:iam::442940292368:policy/roboshopmysqlsecretreader \
#     --approve